#include <stdio.h>
#include <math.h>


int main(){
    int x = 0, y = 0;
    while(x < 50) {
     x = x + 5;
    printf("%i", x);
    if(y <= 8){
    printf("-");
    y++;
    }
      

    }
       return 0;
}